block(`list_queues) {

  alias(`lb:steve:database, `proto),

  clauses(`{
    +service_support:export_queue(index, q)
      <-
      +proto:RequestEnvelope_request[_, index] = union,
      +proto:Request_list_queues[union] = _,
      lb:steve:provision:jobqueue_id[_]=q.
})

} <-- .
